# Audio Engine

The engine chain is:

```text
Tab Capture → EQ → Compressor → Color → Width → Limiter → Output
```

The width module is multiband and phase-aware. Low frequencies stay conservative, while high-frequency side energy receives more stereo sparkle.
